import {Typography} from "@mui/material";

export const H1 = ({children}) => <Typography variant="h1">{children}</Typography>;
export const H2 = ({children}) => <Typography variant="h2">{children}</Typography>;
export const H3 = ({children}) => <Typography variant="h3">{children}</Typography>;
export const H4 = ({children}) => <Typography variant="h4">{children}</Typography>;
